import React from 'react';
import { storiesOf } from '@storybook/react';
import { SearchControls } from './searchControls';

storiesOf('SearchControls', module).add('SearchControls', () => (
  <SearchControls />
));
